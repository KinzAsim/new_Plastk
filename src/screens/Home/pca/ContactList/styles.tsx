import {RF} from '@theme';
import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  alignTopView: {marginTop: RF(10), marginBottom: RF(20)},
  alignTopViewBtn: {marginBottom: RF(30)},

  scrollView: {flexGrow: 1, paddingBottom: RF(100)},
  headerView: {
    paddingTop: RF(0),
    paddingLeft: RF(40),
  },
});
