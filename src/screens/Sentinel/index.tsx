import {AppText, Wrapper} from '@components';
import React from 'react';

const Sentinel = () => {
  return (
    <Wrapper>
      <AppText>haloo</AppText>
    </Wrapper>
  );
};

export default Sentinel;
